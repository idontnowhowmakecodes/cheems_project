# Database

Este módulo gestiona la persistencia de datos.

- Contiene la configuración para conectarse a una base de datos local SQLite (`cheems_project.db`).
- Repositorios (ej. `patient_repository.py`, `session_repository.py`) abstraen y encapsulan las operaciones CRUD.
